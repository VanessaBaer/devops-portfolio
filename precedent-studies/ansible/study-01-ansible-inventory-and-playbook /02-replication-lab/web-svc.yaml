---
- name: webserver setup
  hosts: web01
  become: yes
  tasks:
    - name: install httpd
      ansible.buitin.yum:
        name: httpd
        state: present
    - name: start service
      ansible.builtin.service:
        name: httpd
        state: started
        enabled: yes
        